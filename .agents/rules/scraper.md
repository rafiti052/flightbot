---
paths:
  - scraper.ts
globs:
  - scraper.ts
---

# Scraper contract

Keep the Google Flights selectors, Cheapest-tab behavior, and screenshot capture sequence stable. Preserve the Claude model and extraction prompt. `maxStops` and `maxDurationHours` are authoritative post-extraction filters; the URL `nonstop` query is only a rendering hint.
