{
  "name": "flight-bot",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "bot.js",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "start": "node bot.js",
    "install-browsers": "npx playwright install chromium",
    "dashboard:dev": "npm run dev -w @flightbot/web",
    "web:dev": "npm run dashboard:dev",
    "vercel:preview": "npx --yes vercel deploy --yes",
    "vercel:prod": "npx --yes vercel deploy --prod --yes"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.85.0",
    "@flightbot/shared": "file:packages/shared",
    "express": "^5.2.1",
    "node-cron": "^3.0.3",
    "playwright": "^1.43.0"
  }
}
